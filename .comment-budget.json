{
  "max": 20,
  "maxDensity": 28,
  "_why": "Above the standard's 25 while this stays a published library whose exported types and functions ARE the contract: src/ measures ~41% because every field carries the decision a consumer needs. The threshold applies to the WHOLE repo, which tests and scripts pull down to ~25% — an earlier 40 was set from src-only figures and could therefore never fire. 28 sits just above today's whole-repo reading, so a real climb is still reported. Block length stays at 20 and is what actually fails."
}
